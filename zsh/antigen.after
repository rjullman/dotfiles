#!/usr/bin/env bash

# update antigen (must run in zsh)
`which zsh` <<'EOF'
  source $HOME/.zsh/antigen/antigen.zsh
  source $HOME/.zshrc
  antigen selfupdate &> /tmp/antigen-selfupdate-log
  antigen update &> /tmp/antigen-update-log
EOF
